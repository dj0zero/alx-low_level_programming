#include <stdio.h>

/*
 * main - function prints the name of the file
 * that it was compiled from
 * followed by new line
 * Return: 0
 */

int main(void)
{
	printf("%s\n", _ _FILE_ _);

	return (0);
}
