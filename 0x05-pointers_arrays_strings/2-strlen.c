#include "main.h"

/**
 * _strlen - function returns legnth of a string
 * @s: will be used for *s
 * Return: 0
 */

int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
		return (c);
}
