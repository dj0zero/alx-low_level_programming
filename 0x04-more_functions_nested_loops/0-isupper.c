#include "main.h"

/**
 * _isupper - function checks for uppercase character
 * @c: will be used for character
 * Return: 0
 */

int _isupper(int c)
{

	if (c >= 65 && c <= 98)

	{
	return (1);
	}

	else
	return (0);
}
